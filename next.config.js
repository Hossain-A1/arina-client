/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "randomuser.me","images.unsplash.com","res.cloudinary.com"],
  },
};

module.exports = nextConfig;
